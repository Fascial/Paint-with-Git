[project]
name = "paint-with-git"
version = "0.1.0"
description = "Draw pixel art on your GitHub contribution graph"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "customtkinter>=5.2.2",
]
